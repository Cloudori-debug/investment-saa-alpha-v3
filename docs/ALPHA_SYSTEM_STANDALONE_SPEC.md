# 알파 단독 운용 시스템 (`alpha_system`) — SPEC

> 원본: 사용자 개발 요청서 (2026-07-16)  
> 폐기: `kospi_alpha_tier_weighting_spec.md` 기반 SAA 7 : 알파 3 듀얼 슬리브 프로그램을 **이 트랙의 설계 기준으로 쓰지 않음**.  
> SAA 패시브는 시스템 외부 별도 운영. 본 모듈은 **알파 단독** 판단 보조·규칙 집행 체크만 담당 (자동매매 아님).

## 불변 (확정)

| 항목 | 값 |
|------|-----|
| 알파 자금 한도 | 총자산 대비 **최대 30%** |
| 진입 | **4분할 트랜치** 각 25% (T1 시간 / T2 이벤트 / T3 가격 / T4 혼합) |
| 하드 룰 | ① 소멸 조항 ② 역방향 금지 ③ 논지 훼손 동결 |
| 편입 자격 | **절대 점수 컷오프** (`score_cutoff`) — 상대 순위 금지 |
| 파라미터 | 하드코딩 금지 → `alpha_system/config/` |

## `[TODO]` — config 기본값 비움, 검증으로 강제

유니버스 경계, 종목 수/상한, T2·T3·T4 구체값, `score_cutoff`, 청산 임계값, 벤치마크.

## 개발 순서 (§7)

1. config 스키마 + 트랜치 상태머신 (`entry`) + 하드 룰 3개 ← **이번 범위**
2. 스코어링 이식 + 역할 분리 + 팩터 상관 점검 리포트
3. `exit`
4. `journal` / `report`
5. `sizing` (TODO 값 확정 후)

## 불변 연동 (기존 저장소)

- `data/target_portfolio.csv` 자동 변경 금지
- FASTJUSIK 스크래핑 금지
- 본 패키지는 live target 산출에 **연결하지 않음** (리포트·판정만)
